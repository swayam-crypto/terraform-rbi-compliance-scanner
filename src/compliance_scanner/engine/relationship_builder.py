from .relationship_graph import ResourceGraph
from .resource_index import ResourceIndex


class RelationshipBuilder:

    def build(self, index: ResourceIndex) -> ResourceGraph:
        graph = ResourceGraph()

        #
        # Future PRs will populate the graph
        #

        return graph
